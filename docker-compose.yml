version: '3.9'

services:
  vote:
    image: zeeshankhan001/voting-app:latest
    container_name: vote-app-container
    ports:
      - "80:80"
    depends_on: 
      - redis

  redis:
    image: redis:7-alpine
    container_name: my-redis
    ports:
      - "6379:6379"


